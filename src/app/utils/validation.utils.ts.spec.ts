import { ValidationUtilsTs } from './validation.utils.ts';

describe('ValidationUtilsTs', () => {
  it('should create an instance', () => {
    expect(new ValidationUtilsTs()).toBeTruthy();
  });
});
